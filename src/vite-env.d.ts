/// <reference types="vite/client" />

/** Injected by Vite `define` (git short SHA, or 'dev' locally). Surfaced in the
 *  error modal's technical block so bug reports identify the exact build. */
declare const __APP_VERSION__: string;
